#!/bin/sh
# Dmenu Properties
# -nb color
# -nf color
# -sb color

# launch dmenu
# dmenu_run -nb '#222222' -nf '#eeeeee' -sb '#bbbbbb' -fn "Iosevka"

# Import the colors
. "${HOME}/.cache/wal/colors.sh"

dmenu_run -nb "$color0" -nf "$color15" -sb "$color1" -sf "$color15" -fn "Iosevka"
